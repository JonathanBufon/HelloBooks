Fine summary highlight card. One of the few places the purple gradient fills a whole card (DESIGN.md §16.3) — only when fines are due. `count={0}` shows a clean "Em dia" white card.

```jsx
<FineSummaryCard amount="R$ 24,00" count={2} onPay={...} />
<FineSummaryCard count={0} />
```
