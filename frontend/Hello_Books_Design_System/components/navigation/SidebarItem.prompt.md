A single navigation row used inside `Sidebar`. Active state = light purple fill, purple text, bold.

```jsx
<SidebarItem icon={<LayoutGrid size={18}/>} label="Dashboard" active />
<SidebarItem icon={<AlertCircle size={18}/>} label="Multas" badge={4} />
```
