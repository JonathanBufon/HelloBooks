App top bar — global search at left, notifications/help/avatar cluster at right.

```jsx
<Topbar
  searchPlaceholder="Buscar catálogo, membros ou ISBN..."
  user={{ name: "Marina Costa", role: "Head Librarian" }}
/>
```

Red notification dot shows when `hasNotifications`. Pass `right` to inject extra actions.
