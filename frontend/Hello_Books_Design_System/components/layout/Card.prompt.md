White surface card — the base content container. Rounded 20px, soft purple shadow, 28px padding.

```jsx
<Card>
  <CardHeader title="Atividade Recente" subtitle="Últimas operações" action={<Button variant="ghost" size="sm">Ver tudo</Button>} />
  …conteúdo…
</Card>
```

`elevation`: `none`/`sm`/`md`/`lg`. Pair with `CardHeader` for a titled block.
