Round avatar showing an image, or initials on light purple when no `src`.

```jsx
<Avatar name="Marina Costa" size="lg" />
<Avatar name="João Reis" src="/uploads/joao.jpg" />
```

Sizes: `sm` 32, `md` 40, `lg` 48, `xl` 64px.
