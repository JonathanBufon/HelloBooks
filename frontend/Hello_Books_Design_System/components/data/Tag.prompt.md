Category/classification chip in light purple. Use for tags/categorias — not status (use StatusBadge for that).

```jsx
<Tag>Acadêmico</Tag>
<Tag>Ficção Histórica</Tag>
<Tag onRemove={() => ...}>Mistério</Tag>
```

`hash` (default true) prepends `#`. `onRemove` adds a dismiss button.
