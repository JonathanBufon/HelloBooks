Multi-line text field for synopses, internal notes, observations.

```jsx
<Textarea label="Sinopse" rows={5} placeholder="Resumo da obra…" />
```

Same visual language as `TextInput`: label above, purple focus halo, `error`/`hint` below.
