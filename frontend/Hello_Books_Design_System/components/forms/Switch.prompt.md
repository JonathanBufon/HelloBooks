Toggle switch for policy and setting flags. `onChange` receives the next boolean.

```jsx
<Switch label="Permitir renovação" checked={allow} onChange={setAllow} />
<Switch label="Disponível no catálogo público" checked />
```
